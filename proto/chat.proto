syntax = "proto3";

package chat;

option go_package = "grpc-chat/proto";

message ChatMessage {
  string username = 1;
  string content = 2;
  bool history = 3;
}

service ChatService {
  rpc Chat(stream ChatMessage) returns (stream ChatMessage);
}
