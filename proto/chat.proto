syntax = "proto3";

package chat;

option go_package = "grpc-chat/proto";

message ChatMessage {
  string username = 1;
  string content = 2;
}

message Empty {}

service ChatService {
  rpc SendMessage(ChatMessage) returns (Empty);
}